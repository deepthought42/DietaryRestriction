<p>
	<span>Ingredients</span>
	<% for ingredient in Ingredient.find(:all) %>
	  <div>
		<%= check_box_tag "product[ingredient_ids][]", ingredient.id, @product.ingredients.include?(ingredient) %>
	    <%= ingredient.name %>
	  </div>
	<% end %>
</p>