<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :brand, { :class => (@product.brand ? "existingInput" : "newInputField"), :value => @product.brand || "Brand"} %>
  </div>
   <div class="field">
    <%= f.text_field :name, { :class => (@product.name ? "existingInput" : "newInputField"), :value => @product.name || "Product Name"} %>
  </div>
  <div class="field">
    <%= f.text_area :description, {:rows => "3", :class => (@product.description ? "existingInput" : "newInputField"), :value => @product.description || "Product Description"} %>
  </div>
  
  <%= render "ingredient_fields" %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
